const parsentMath = document.getElementById('parsentMath');
console.log(parsentMath);
const width = document.getElementById('width');
console.log(width)
const Math = parsentMath.textContent;
const para = Number(Math);
console.log(para);
const ans = para + '%';
width.style.width = `${para}%`;
// const parsentConte = document.docmen

// function